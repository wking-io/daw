/**
 * Data attributes applied to Popover components.
 */
export enum PopoverRootDataAttributes {
  /**
   * Indicates the open/closed state of the popover.
   * @type {'open' | 'closed'}
   */
  state = "data-state",
}
