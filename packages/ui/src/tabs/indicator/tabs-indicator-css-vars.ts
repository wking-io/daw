/**
 * CSS custom properties set by the TabsIndicator component.
 */
export enum TabsIndicatorCssVars {
  /**
   * Left position of the active tab in pixels.
   */
  activeTabLeft = "--active-tab-left",
  /**
   * Right position of the active tab in pixels.
   */
  activeTabRight = "--active-tab-right",
  /**
   * Top position of the active tab in pixels.
   */
  activeTabTop = "--active-tab-top",
  /**
   * Bottom position of the active tab in pixels.
   */
  activeTabBottom = "--active-tab-bottom",
  /**
   * Width of the active tab in pixels.
   */
  activeTabWidth = "--active-tab-width",
  /**
   * Height of the active tab in pixels.
   */
  activeTabHeight = "--active-tab-height",
  /**
   * Calculated transition duration in milliseconds.
   */
  indicatorDuration = "--indicator-duration",
}
