/**
 * Data attributes for DialogRoot state.
 */
export enum DialogRootDataAttributes {
  /**
   * Present when the dialog is open.
   */
  open = "data-open",
  /**
   * Present when the dialog is closed.
   */
  closed = "data-closed",
}
