/**
 * Data attributes for DialogBackdrop state.
 */
export enum DialogBackdropDataAttributes {
  /**
   * Present when the dialog is open.
   */
  open = "data-open",
  /**
   * Present when the dialog is closed.
   */
  closed = "data-closed",
}
